services:
    redis:
        image: redis:latest
        container_name: my-redis
        ports:
            - '6379:6379'
        volumes:
            - redis-data:/data
        command: redis-server --requirepass my_redis_password --appendonly yes
        restart: unless-stopped

volumes:
    redis-data:
        driver: local
